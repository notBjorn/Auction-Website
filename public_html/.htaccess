# --- Execute CGI for Python (and optionally Perl/CGI) ---
Options +ExecCGI -Indexes
AddHandler cgi-script .py
# If you truly have Perl/other CGI scripts, keep these; otherwise delete:
# AddHandler cgi-script .pl
# AddHandler cgi-script .cgi

# --- Default index file ---
DirectoryIndex index.html

# --- Optional: force HTTPS (good practice; comment out if it causes issues) ---
RewriteEngine On
RewriteCond %{HTTPS} !=on
RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

